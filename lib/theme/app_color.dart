import 'package:flutter/material.dart';

class AppColor {
  // 默认主题种子色 从种子色生成配色方案，保持视觉一致性
  static const Color defaultSeed = Color(0xFF255BA3);
  // 默认全局背景色
  static const Color defaultBg = Color(0xffF6F8F7);
}