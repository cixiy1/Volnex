// 设置页（开发骨架占位）
//
// 说明：当前为空白开发骨架，仅保留一行占位文字。
// 后续可在此接入主题、语言、关于等设置项。
import 'package:flutter/material.dart';

/// 设置页面组件（开发骨架占位）
class SettingsPage extends StatelessWidget {
  const SettingsPage({super.key});

  @override
  Widget build(BuildContext context) => const Center(child: Text('设置（开发中）'));
}
